@model IEnumerable<THIS_Hospital.CauseType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CauseTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CauseTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CauseTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CauseTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CauseTypeID })
        </td>
    </tr>
}

</table>
