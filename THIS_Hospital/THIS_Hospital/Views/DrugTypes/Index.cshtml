@model IEnumerable<THIS_Hospital.DrugType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Desecription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Desecription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DrugTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DrugTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DrugTypeID })
        </td>
    </tr>
}

</table>
