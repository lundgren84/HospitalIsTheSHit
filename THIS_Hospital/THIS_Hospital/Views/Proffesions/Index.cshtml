@model IEnumerable<THIS_Hospital.Proffesion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Profession_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Profession_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProffesionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProffesionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProffesionID })
        </td>
    </tr>
}

</table>
